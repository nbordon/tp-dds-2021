<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="../css/estilos.css">

    <script
            src="https://code.jquery.com/jquery-3.6.0.min.js"
            integrity="sha256-/xUj+3OJU5yExlq6GSYGSHk7tPXikynS7ogEvDej/m4="
            crossorigin="anonymous"></script>
        <title>Mascota a adoptar</title>
</head>
<body>
{{>navbar.hbs}}
<div class="container bg-secondary my-5 shadow-lg">
    <div class="row">

        <h1 class="h1 text-center">Comodidades para adoptar una mascota</h1>

        <form class=""

              action="/AdopcionPorDuenio/{{idMascota}}" method="post"   >
            <div class="row">





                {{#each preguntas }}
                    <div class="col-4 my-2">
                        <label>
                            {{descripcion}}
                            <select class="form-select" aria-label="Default select example"
                                    name="{{id}}">
                                <option selected>Seleccionar un valor</option>
                                {{#each valor}}
                                    <option  value="{{this}}">{{this}}</option>
                                {{/each}}
                            </select>
                        </label>
                    </div>
                {{/each}}

                <div class="row justify-content-center d-flex">


                    <div class="col-2 my-2">
                        <input type="submit" name="" class="btn btn-primary rounded-pill text-secondary"
                               style="float:right;" value="Enviar">
                    </div>

                </div>
            </div>
        </form>
    </div>
</div>
</body>
</html>