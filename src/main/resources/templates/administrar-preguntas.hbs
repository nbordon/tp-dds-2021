<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.6.1/font/bootstrap-icons.css">
    <link rel="stylesheet" href="css/estilos.css">
    <script src="https://code.jquery.com/jquery-3.6.0.min.js" integrity="sha256-/xUj+3OJU5yExlq6GSYGSHk7tPXikynS7ogEvDej/m4=" crossorigin="anonymous"></script>
    <script src="/js/generales.js"></script>
    <title>Preguntas de adopción</title>
</head>
<body>
<div class="container-fluid">
    <div class="row flex-nowrap">
        {{#if usuario}}
            <div class="col-2 bg-dark">
                {{>menu.hbs}}
            </div>
        {{/if}}
        <div class="col-10 px-0">
            {{>navbar.hbs}}
            <div class="row px-0 my-3 mx-5">
                <div class="col-6">
                    <h3>Administrar Preguntas</h3>
                </div>

                <div class="col-6">
                    <a href="/pregunta" class="btn btn-primary rounded-pill">Agregar Pregunta</a>
                </div>
            </div>
            <div class="row px-0 my-3 mx-5">
                <div class="col">
                    <table class="table">
                        <thead class="table-primary">
                        <tr>
                            <th></th>
                            <th>#</th>
                            <th>Pregunta</th>
                        </tr>
                        </thead>
                        <tbody>
                        {{#each preguntas }}
                            <tr>
                                <th>
                                    <a href="pregunta/{{id}}"><i class="bi bi-search px-2 text-primary"></i></a>
                                    <a href="editar-pregunta/{{id}}"><i class="bi bi-pencil-fill px-2 text-warning"></i></a>
                                    <button type="button" class="btn" onclick="confirmarEliminacionDePregunta('{{id}}')"><i class="bi bi-trash-fill px-2 text-danger"></i></button>
                                </th>
                                <th>{{&increment @index}}</th>
                                <th>{{descripcion}}</th>
                            </tr>
                        {{/each}}
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    </div>
</div>

<script src="libs/bootstrap/dist/js/bootstrap.bundle.js"></script>
</body>

</html>